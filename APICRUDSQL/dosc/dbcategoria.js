var config = require("./dbcopnfig"); // instanciamos el archivo dbconfig
const sql = require("mysql"); //se necesirta paquete mssql

async function getCategoria() {
  try {
  } catch (error) {
    console.log(error);
  }
}
